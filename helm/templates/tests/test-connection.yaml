apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hawtio.fullname" . }}-test-connection"
  labels:
    {{- include "hawtio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hawtio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

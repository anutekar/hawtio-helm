apiVersion: v1
kind: Service
metadata:
  name: {{ include "hawtio.fullname" . }}
  labels:
    {{- include "hawtio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hawtio.selectorLabels" . | nindent 4 }}
